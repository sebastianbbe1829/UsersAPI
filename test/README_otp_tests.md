# OTP test coverage

This file documents the test scope for the generic OTP, account activation OTP, and password recovery OTP flows.

The executable tests live in `test/test_otp.py`, `test/test_activation_otp.py`, and `test/test_password_recovery.py`.
